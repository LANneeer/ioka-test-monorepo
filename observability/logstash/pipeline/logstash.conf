input {
  tcp {
    port => 5044
    codec => json_lines
  }
}
filter {
  mutate {
    add_field => { "stack" => "local" }
  }
}
output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "user-service-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
